{
  "description": "SessionStart: inject the karpathy-ponytail coding guidelines (full ladder, no lite/ultra) as additionalContext. No matcher — fires on every source (startup/resume/clear/compact) since /clear and /compact wipe context same as a fresh startup; do not restrict to 'startup'.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [{ "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/inject-ponytail-guidelines.mjs" }]
      }
    ]
  }
}
