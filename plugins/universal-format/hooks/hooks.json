{
  "description": "PreToolUse Write|Edit -> format_pre: prettier languages (jsts/json/yaml/markdown/css/scss) formatted in-process before the write with the prettier bundled into this plugin's MCP server. PostToolUse Write|Edit -> format_post: shell/java/kotlin/python/go/php formatted on disk after the write via each tool's CLI.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [{ "type": "mcp_tool", "server": "plugin:universal-format:universal-format-hooks", "tool": "format_pre", "timeout": 60 }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [{ "type": "mcp_tool", "server": "plugin:universal-format:universal-format-hooks", "tool": "format_post", "timeout": 60 }]
      }
    ]
  }
}
