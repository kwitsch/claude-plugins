# CLAUDE.md — memory-enhancement

A dream skill plus a Stop/SessionStart hook pair that nudges the next session to run one.

## Behavior
<what the plugin's component(s) do at runtime — key rules, guards, edge cases>.

## Tests
`test/memory-enhancement/test.bats` (bats). Run: `BATS_LIB_PATH=/usr/lib/bats bats test/memory-enhancement/`.
