{
  "name": "branch-management",
  "version": "4.0.0",
  "description": "Branch lifecycle skills. new-branch cuts a fresh branch inline; new-pr runs iterative parallel review rounds (claude/codex/copilot/coderabbit reviewers) backed by subagents, applies verified fixes between rounds, opens a PR/MR via gh or glab, and watches CI until green.",
  "author": {
    "name": "Kwitsch"
  },
  "userConfig": {
    "review_claude": {
      "type": "boolean",
      "title": "Claude reviewer",
      "description": "Values: true = run the claude-reviewer subagent during new-pr review rounds; false = skip it. Default: true.",
      "default": true
    },
    "review_codex": {
      "type": "boolean",
      "title": "Codex reviewer",
      "description": "Values: true = run the codex-reviewer subagent (requires codex CLI login); false = skip it. Default: true.",
      "default": true
    },
    "review_copilot": {
      "type": "boolean",
      "title": "Copilot reviewer",
      "description": "Values: true = run the copilot-reviewer subagent (requires copilot CLI login); false = skip it. Default: true.",
      "default": true
    },
    "review_coderabbit": {
      "type": "boolean",
      "title": "CodeRabbit reviewer",
      "description": "Values: true = run the coderabbit-reviewer subagent (requires coderabbit CLI login); false = skip it. Default: true.",
      "default": true
    },
    "ci_monitor": {
      "type": "boolean",
      "title": "CI monitoring",
      "description": "Values: true = watch CI results after the PR/MR is opened; false = stop after opening the PR/MR. Default: true.",
      "default": true
    },
    "ci_watch_timeout": {
      "type": "number",
      "title": "CI watch timeout (seconds)",
      "description": "Values: positive whole-number seconds for the CI watch deadline; invalid or missing values fall back to 1800. Default: 1800.",
      "default": 1800
    },
    "coderabbit_ci_comments": {
      "type": "boolean",
      "title": "CodeRabbit CI comments",
      "description": "Values: true = collect and address open CodeRabbit bot comments while watching CI; false = ignore them. Default: true.",
      "default": true
    },
    "delete_branch_on_merge": {
      "type": "boolean",
      "title": "Delete branch on merge",
      "description": "Values: true = after opening the PR/MR, enable automatic deletion of the head/source branch when it merges (GitHub: sets the repo delete_branch_on_merge setting via gh api, idempotent, soft-fails without admin; GitLab: passes --remove-source-branch); false = leave branch-deletion settings untouched. Default: true.",
      "default": true
    },
    "rebase_before_pr": {
      "type": "boolean",
      "title": "Rebase onto base before PR",
      "description": "Values: true = in new-pr, after committing and before pushing, rebase the work branch onto origin/<base> when the base branch (usually main) gained new upstream commits (force-with-lease push; stops on conflict); false = skip the rebase. Default: true.",
      "default": true
    },
    "review_max_rounds": {
      "type": "number",
      "title": "Max review rounds",
      "description": "Values: positive whole-number cap on iterative review rounds; invalid or missing values fall back to 3. Default: 3.",
      "default": 3
    }
  }
}
