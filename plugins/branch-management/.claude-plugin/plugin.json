{
  "name": "branch-management",
  "version": "3.1.0",
  "description": "Branch lifecycle skills backed by subagents: new-branch cuts a fresh branch; new-pr runs iterative parallel review rounds (claude/codex/copilot/coderabbit reviewers), applies verified fixes between rounds, opens a PR/MR via gh or glab, and watches CI until green. Optionally refreshes a graphify output folder on new-branch and before the PR push (separate commit). Script execution and heavy output processing run through the context-mode plugin (declared cross-marketplace dependency).",
  "author": {
    "name": "Kwitsch"
  },
  "dependencies": [
    {
      "name": "context-mode",
      "marketplace": "context-mode"
    }
  ],
  "userConfig": {
    "review_claude": {
      "type": "boolean",
      "title": "Claude reviewer",
      "description": "Run the claude-reviewer subagent during new-pr review rounds",
      "default": true
    },
    "review_codex": {
      "type": "boolean",
      "title": "Codex reviewer",
      "description": "Run the codex-reviewer subagent (requires codex CLI login)",
      "default": true
    },
    "review_copilot": {
      "type": "boolean",
      "title": "Copilot reviewer",
      "description": "Run the copilot-reviewer subagent (requires copilot CLI login)",
      "default": true
    },
    "review_coderabbit": {
      "type": "boolean",
      "title": "CodeRabbit reviewer",
      "description": "Run the coderabbit-reviewer subagent (requires coderabbit CLI login)",
      "default": true
    },
    "ci_monitor": {
      "type": "boolean",
      "title": "CI monitoring",
      "description": "Watch CI results after the PR/MR is opened (new-pr final phase)",
      "default": true
    },
    "context_index": {
      "type": "boolean",
      "title": "context-mode indexing",
      "description": "Refresh the context-mode index after creating a new branch",
      "default": true
    },
    "coderabbit_ci_comments": {
      "type": "boolean",
      "title": "CodeRabbit CI comments",
      "description": "Collect and address open CodeRabbit bot comments while watching CI",
      "default": true
    },
    "graphify_branch_update": {
      "type": "boolean",
      "title": "graphify update on new branch",
      "description": "Refresh the graphify output (graphify update .) after creating a new branch",
      "default": true
    },
    "graphify_force_create": {
      "type": "boolean",
      "title": "graphify force create",
      "description": "new-branch only: create a missing graphify-out/ folder and run the update anyway. Off by default so a missing folder is never created accidentally — only an explicit true enables this.",
      "default": false
    },
    "graphify_pr_update": {
      "type": "boolean",
      "title": "graphify update before PR",
      "description": "Refresh the graphify output after the review rounds and before pushing in new-pr (requires an existing graphify-out/ folder)",
      "default": true
    },
    "graphify_pr_commit": {
      "type": "boolean",
      "title": "graphify separate commit",
      "description": "Commit refreshed graphify-out files as a separate commit during new-pr. When disabled, updated files are left uncommitted in the working tree.",
      "default": true
    },
    "graphify_user_files": {
      "type": "boolean",
      "title": "graphify human-only outputs",
      "description": "Keep human-only graphify outputs (graph.html) after updates. Off by default — the graphify output serves agents, so the refresh prunes browser-only files; only an explicit true keeps them.",
      "default": false
    }
  }
}
