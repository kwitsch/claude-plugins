{
  "name": "taskflow",
  "version": "1.7.1",
  "description": "Spec-driven design & delivery pipeline. /taskflow:build-task orchestrates two dynamic workflows: design-to-spec (explore, draft, review, spec) and spec-driven-delivery (plan, wave-parallel implement in isolated worktrees, per-wave merge, combined review, fix application). /taskflow:changes-audit runs the review pass standalone with optional --fix.",
  "author": {
    "name": "Kwitsch"
  }
}
