{
  "description": "PostToolUse Write|Edit -> lint-file.mjs: runs the just-written file's standard linter (check-only) and surfaces findings as context.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/lint-file.mjs",
            "timeout": 95,
            "async": true
          }
        ]
      }
    ]
  }
}
