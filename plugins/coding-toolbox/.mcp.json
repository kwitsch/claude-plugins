{
  "mcpServers": {
    "coding-toolbox-hooks": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/mjs-launch.sh",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"],
      "env": {
        "CODING_TOOLBOX_WORKTREE_REFRESH": "${user_config.worktree_refresh}"
      }
    }
  }
}
