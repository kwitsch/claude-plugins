# Local planning artifacts (superpowers specs/plans) — kept local, never pushed.
# Safe to delete after the related PR is merged.
docs/

# graphify knowledge graph output — local development only, never pushed.
graphify-out/

# codebase-memory-mcp persisted graph artifact — local development only, never pushed.
.codebase-memory/

# npm (bats test framework)
node_modules/

# caveman-compress backups of compressed memory files — local only.
*.original.md

# Agent runtime artifacts — local only, never pushed.
.claude/agent-memory/
**/.orphaned_at

# headroom MCP memory store — local only, never pushed.
.headroom/

# Rust build output for the linux-token-efficiency MCP crate — local only; the committed
# artifact lives at plugins/linux-token-efficiency/mcp/linux-token-efficiency-mcp instead.
src/linux-token-efficiency-mcp/target/
